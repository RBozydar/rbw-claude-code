Read my entire instruction setup before responding. Check my root `CLAUDE.md`, any nested `CLAUDE.md` files, all relevant skill files, context folders, and any other instruction files you can find that materially affect how you work in this project.

Then review every rule, instruction, and preference you found. For each one, tell me:
1. Is this something you already do by default without being told?
2. Does this contradict another rule somewhere else in the setup?
3. Does this duplicate something already covered by a different file?
4. Does this look like a bandaid added to fix one bad output rather than improve performance overall?
5. Is it vague enough that it could be interpreted differently every time?

Then give me:
- a list of everything you would cut, with a one-line reason for each
- a list of anything you would rewrite or merge, with reasons
- a list of conflicts you found between files
- a cleaned-up version of my root `CLAUDE.md` with dead weight removed
- a short summary of how many rules passed cleanly vs got flagged

Do not change any files yet. Just report.