name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: markdownlint
        uses: DavidAnson/markdownlint-cli2-action@v19

  codex-agents:
    name: Codex Agents
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6

      - name: Run Codex conversion tests
        run: uv run pytest tests/test_convert_codex.py -q

      - name: Regenerate Codex agents
        run: uv run python scripts/generate_codex_agents.py

      - name: Check generated Codex agents are up to date
        run: |
          if [[ -n "$(git status --short -- .codex/agents)" ]]; then
            git status --short -- .codex/agents
            echo "Generated Codex agents are out of date. Run: uv run python scripts/generate_codex_agents.py"
            exit 1
          fi

  ci-success:
    name: All CI Checks Passed
    runs-on: ubuntu-latest
    needs: [lint, codex-agents]
    if: always()
    steps:
      - name: Check all jobs status
        run: |
          if [[ "${{ needs.lint.result }}" != "success" ]]; then
            echo "CI job failed"
            exit 1
          fi
          if [[ "${{ needs.codex-agents.result }}" != "success" ]]; then
            echo "CI job failed"
            exit 1
          fi
          echo "All CI checks passed!"
